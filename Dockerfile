# Используйте официальный образ Golang
FROM golang:latest

# Установите рабочую директорию внутри контейнера
WORKDIR /go/src/app

# Скопируйте все файлы из текущей директории внутрь контейнера
COPY . .

# Соберите приложение Go
RUN go build -o main ./cmd/mail

# Экспортируйте порт, на котором будет работать приложение
EXPOSE 8080

# Команда для запуска приложения при старте контейнера
CMD ["./main"]